---
import { skills } from '../data/skills';

const skillCategories = {
  frontend: skills.filter(skill => skill.category === 'frontend'),
  backend: skills.filter(skill => skill.category === 'backend'),
  database: skills.filter(skill => skill.category === 'database'),
  tools: skills.filter(skill => skill.category === 'tools'),
  soft: skills.filter(skill => skill.category === 'soft')
};

const categoryTitles = {
  frontend: 'Frontend Development',
  backend: 'Backend Development',
  database: 'Databases',
  tools: 'Tools and Technologies',
  soft: 'Soft Skills'
};

const categoryColors = {
  frontend: 'from-blue-500 to-purple-600',
  backend: 'from-green-500 to-teal-600',
  database: 'from-yellow-500 to-orange-600',
  tools: 'from-red-500 to-pink-600',
  soft: 'from-indigo-500 to-blue-600'
};
---

<section id="skills" class="py-16 bg-gray-50">
  <div class="container mx-auto px-6">
    <div class="text-center mb-12">
      <h2 class="text-4xl md:text-5xl font-bold text-gray-800 mb-6">Skills & Technologies</h2>
      <div class="w-24 h-1 bg-primary-600 mx-auto mb-4"></div>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto">
        A comprehensive overview of my technical skills and expertise
      </p>
    </div>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      {Object.entries(skillCategories).map(([category, categorySkills]) => (
        <div class="bg-white rounded-xl shadow-md p-5 hover:shadow-lg transition-all duration-300">
          <div class={`w-12 h-12 rounded-xl bg-gradient-to-r ${categoryColors[category]} flex items-center justify-center mb-4`}>
            <span class="text-white text-xl">
              {category === 'frontend' ? '🎨' : 
               category === 'backend' ? '⚙️' : 
               category === 'database' ? '💾' : 
               category === 'tools' ? '🛠️' : '🤝'}
            </span>
          </div>
          
          <h3 class="text-lg font-semibold text-gray-800 mb-4">
            {categoryTitles[category]}
          </h3>
          
          <div class="grid grid-cols-1 gap-2">
            {categorySkills.map(skill => (
              <div class="flex items-center space-x-3 p-2 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors duration-200">
                <div class="w-6 h-6 bg-primary-100 rounded-md flex items-center justify-center flex-shrink-0">
                  <span class="text-primary-600 text-xs font-bold">
                    {skill.name.charAt(0)}
                  </span>
                </div>
                <span class="text-gray-800 font-medium text-sm">{skill.name}</span>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>

    <!-- Skills summary -->
    <div class="mt-12 text-center">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        <div class="bg-white rounded-xl p-4 shadow-md hover:shadow-lg transition-shadow duration-300">
          <div class="text-2xl font-bold text-primary-600 mb-1">15+</div>
          <div class="text-gray-600 font-medium text-sm">Technologies</div>
        </div>
        <div class="bg-white rounded-xl p-4 shadow-md hover:shadow-lg transition-shadow duration-300">
          <div class="text-2xl font-bold text-primary-600 mb-1">5+</div>
          <div class="text-gray-600 font-medium text-sm">Frameworks</div>
        </div>
        <div class="bg-white rounded-xl p-4 shadow-md hover:shadow-lg transition-shadow duration-300">
          <div class="text-2xl font-bold text-primary-600 mb-1">3+</div>
          <div class="text-gray-600 font-medium text-sm">Databases</div>
        </div>
        <div class="bg-white rounded-xl p-4 shadow-md hover:shadow-lg transition-shadow duration-300">
          <div class="text-2xl font-bold text-primary-600 mb-1">100%</div>
          <div class="text-gray-600 font-medium text-sm">Dedication</div>
        </div>
      </div>
    </div>
  </div>
</section>